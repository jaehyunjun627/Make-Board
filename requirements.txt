# from-json 명령만 쓰고 결과를 CSV로 받을 거라면 아무것도 설치하지 않아도 됩니다.
# (JSON 읽기·CSV 쓰기는 파이썬 기본 기능만 사용합니다)

# 결과를 시트 3개짜리 엑셀 파일(.xlsx) 하나로 받으려면 필요합니다.
# 없으면 자동으로 CSV 파일 여러 개로 나눠 저장합니다.
openpyxl>=3.1.0

# crawl / login 명령(브라우저 직접 수집)을 쓰려면 필요합니다.
playwright>=1.44.0
